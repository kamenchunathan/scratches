executable(
  'manip',
  'manip.cpp',
  install: false,
  dependencies: [
    dependency('osmanip', fallback: ['osmanip', 'osmanip_dep'], required: true),
  ],
)

executable(
  'iterators',
  'iterators.cpp',
  install: false,
  dependencies: [],
)
